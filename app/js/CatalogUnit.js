/*
 * Immutable Unit data
 */

// boolean storeObj - true=store object on backend, false=do not
function CatalogUnit(name, type, city) // Constructor
{
    this.name = name;
    this.type = type;
    this.city = city;
}
